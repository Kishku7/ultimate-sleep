org.gradle.jvmargs=-Xmx2G
# Loom 1.16 requires a JVM 21+ runtime (JDK17 daemon fails at configuration time), so the daemon
# is pinned to JDK 21; options.release=17 in build.gradle keeps the emitted bytecode Java 17 as
# 1.20-1.20.4 requires. (Pinned so the build does not pick the system JDK25.)
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-21.0.9.10-hotspot

minecraft_version=1.20.1
loader_version=0.18.6

mod_version=1.2.0
maven_group=com.kishku7
archives_base_name=ultimate-sleep

fabric_api_version=0.92.6+1.20.1
mc_dep=>=1.20- <1.20.5
